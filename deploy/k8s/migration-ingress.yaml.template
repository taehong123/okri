# A single-use protected ingress for copying the existing Sites state to the
# self-hosted importer. It is intentionally excluded from kustomization and
# must be deleted immediately after the transfer succeeds.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: okri-migration
  namespace: okri
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - okri-migrate.orderflow.kr
      # Copied only for this short-lived ingress from the existing
      # orderflow namespace wildcard certificate.
      secretName: orderflow-tls
  rules:
    - host: okri-migrate.orderflow.kr
      http:
        paths:
          - path: /api/internal/migration-receiver
            pathType: Exact
            backend:
              service:
                name: okri
                port:
                  name: http
