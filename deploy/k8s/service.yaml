apiVersion: v1
kind: Service
metadata:
  name: okri
  labels:
    app.kubernetes.io/name: okri
spec:
  selector:
    app.kubernetes.io/name: okri
  ports:
    - name: http
      port: 3000
      targetPort: http
