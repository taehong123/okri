{
  "drizzle/0055_daily_work_status.sql": {
    "sha256": "98f08987e7e7e6b15f88e8535c6cbe87a68cfc66d0160747332eba8e1c3b5cf8",
    "v1Compatibility": "The three added NOT NULL columns have defaults, so existing v1 inserts remain valid. Legacy skip cancellation and remote-status preservation are covered by regression tests.",
    "rollbackPlan": "Keep the added columns and their data when rolling back application code. Older code ignores them; do not drop columns or rewrite existing migration history.",
    "dataPreservation": "ADD COLUMN with defaults preserves record IDs, assignments, drafts and submission history. The migration contains no deletes or destructive table replacement.",
    "reviewedBy": "Codex native compatibility integration review, 2026-09-08"
  },
  "drizzle/0063_native_sessions.sql": {
    "sha256": "0ca44e1a63fcb784e10f604f421b45322c024be5f863a804c7a66394cd0e5844",
    "v1Compatibility": "Replays the unchanged additive native schema from the frozen 0054 fixture after the currently deployed web migrations. IF NOT EXISTS preserves installed schema; the account-deletion safety CHECK remains enforced.",
    "rollbackPlan": "Leave the native tables and indexes in place on application rollback. Do not delete sessions, alter previous migrations, or remove the account-deletion guard.",
    "dataPreservation": "Only CREATE TABLE/INDEX IF NOT EXISTS statements are executed. ON DELETE CASCADE is the existing user-owned session foreign-key policy, not a deletion command. No existing records are mutated.",
    "reviewedBy": "Codex native compatibility integration review, 2026-09-10"
  }
}
