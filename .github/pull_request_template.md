## Change

## Installed Mobile Clients

- Native disposition: shared next bundle / native implemented / web-only / deferred
- Update mobile/release/parity.md for product changes.
- Oldest supported client contract and auth tests:
- API/DB change is additive; rollback compatibility:
- Store binary or web/server-only change:

## Verification

Web tests, native contracts, device evidence where required. Never claim a local
build or TestFlight/internal upload is a public release.
